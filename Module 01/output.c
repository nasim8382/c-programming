#include <stdio.h>

int main()
{
    // int nahiyan;
    // scanf("%d", &nahiyan);
    // printf("%d", nahiyan);
    
    // int ping, pong;
    // scanf("%d %d", &ping, &pong);
    // printf("%d", ping + pong);

    // float a, b;
    // scanf("%f %f", &a, &b);
    // printf("%0.2f", a * b);

    // char a, b, c, d, e;
    // scanf("%c %c %c %c %c", &a, &b, &c, &d, &e);
    // printf("%c%c%c%c%c", a, b, c, d, e);

    int a, b;
    // char p;
    // scanf("%d%c %d%c", &a, &p, &b, &p);
    scanf("%d%% %d%%", &a, &b);
    // printf("%d%c %d%c", a, p, b, p);
    printf("%d%% %d%%", a, b);

    return 0;
}